{
  "compilerOptions": {
    "baseUrl": ".",             // ✅ baseUrl harus ada
    "paths": {
      "@/*": ["src/*"],         // alias @ -> ke dalam src/
      "~helper/*": ["helper/*"] // alias ~helper -> ke luar src/
    }
  }
}
